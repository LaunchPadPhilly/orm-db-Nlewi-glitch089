// Legacy test file replaced by `form.test.jsx` (JSX-aware).
// Keep a no-op skipped test here to avoid duplicate JSX parse errors.
import { it } from 'vitest'

it.skip('legacy form test placeholder', () => {})